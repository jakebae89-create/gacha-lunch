.claude/

# Node / Capacitor
node_modules/
www/

# 심사용 화면 녹화 — 100MB가 넘어 GitHub push 자체가 거부된다
*.MP4
*.mp4
*.mov
*.MOV

# 심사용 기기 스크린샷 — 지도에 실제 위치가 찍혀 있어 공개 저장소에 두면 안 된다
# (스토어 등록용 store-assets/ 는 커밋 대상이므로 ios/ 안쪽만 막는다)
ios/*.PNG
ios/*.png

# 서명키 — 절대 커밋 금지 (앱의 평생 열쇠 + 비밀번호)
*.jks
*.keystore
upload-keystore.jks
key.properties
android/key.properties
