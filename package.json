{
  "name": "gacha-lunch",
  "version": "1.0.0",
  "private": true,
  "description": "가챠밥 - 캡슐 가챠로 점심 식당 뽑기 앱 (Capacitor + AdMob)",
  "type": "module",
  "scripts": {
    "build:www": "node scripts/build-www.mjs",
    "sync": "npm run build:www && cap sync android",
    "open:android": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor-community/admob": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
